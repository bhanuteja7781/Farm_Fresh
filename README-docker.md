# FarmFresh Docker Environment Setup

To run FarmFresh locally with Docker Compose:

```bash
docker-compose up -d --build
```

Access application:
- Web App: `http://localhost`
- Backend API: `http://localhost/api/`
